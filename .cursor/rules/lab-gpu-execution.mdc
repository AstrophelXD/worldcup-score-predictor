---
description: 训练与 GPU 任务在实验室 RTX 4090 主机上执行，不在本地直接运行
alwaysApply: true
---

# 实验室 GPU 主机执行

本项目的训练、回测、GPU 推理等重计算任务 **不在 Cursor 所在本地环境直接运行**。

## 必须遵守

- 不要在本机执行训练脚本、大规模数据处理、模型回测、依赖 CUDA/GPU 的命令。
- 不要启动长时间占用 GPU 或大量内存的进程。
- 需要验证时，优先给出可在实验室 RTX 4090（24GB）主机上执行的完整命令与步骤，由用户自行运行。

## 应提供的指令内容

给出命令时至少包含：

1. **环境准备**：conda/venv 激活、依赖安装、`cd` 到项目目录。
2. **运行命令**：完整、可复制的一行或多行命令（含必要参数）。
3. **预期输出**：成功标志、关键日志或产物路径。
4. **失败排查**：常见错误与检查项（CUDA 可见性、显存、数据路径等）。

## 本地可做的轻量操作

以下可在本地进行，无需 GPU：

- 阅读/编写代码与文档
- 静态检查、格式化、单元测试（不依赖 GPU）
- 小规模 dry-run（如 `--help`、配置校验、样例数据 smoke test）

## 硬件假设

- 目标执行环境：实验室配备 **RTX 4090 24GB** 的单机单卡主机。
- 默认启用 mixed precision；高级模型优先适配单卡训练。
