---
description: 重计算与 GPU 任务优先给出可复制命令，并说明资源需求
alwaysApply: true
---

# GPU 与重计算任务

训练、回测、大规模数据处理等任务可能依赖 CUDA 与较大显存。

## 必须遵守

- 执行训练或 GPU 任务前，先确认环境是否有可用 GPU 与足够显存。
- 不要启动超长时间、超大批量的训练而不告知用户资源需求。
- 需要验证时，给出完整、可复制的命令与步骤，并说明预期产物路径。

## 应提供的指令内容

给出命令时至少包含：

1. **环境准备**：conda/venv 激活、依赖安装、`cd` 到项目目录。
2. **运行命令**：完整、可复制的一行或多行命令（含必要参数）。
3. **预期输出**：成功标志、关键日志或产物路径。
4. **失败排查**：常见错误与检查项（CUDA 可见性、显存、数据路径等）。

## 本地可做的轻量操作

以下通常可在普通开发机进行，无需 GPU：

- 阅读/编写代码与文档
- 静态检查、格式化、单元测试（不依赖 GPU）
- 小规模 dry-run（如 `--help`、配置校验、样例数据 smoke test）

## 硬件参考

- Baseline / Mid-level：CPU 或小显存 GPU 即可。
- ScoreGen 全量：建议 16GB+ 显存；显存不足时使用 `models=scoregen_local` 降低 batch。
- 默认启用 mixed precision（bf16）以节省显存。
